# Simple Blog

This is a simple Node.js project demonstrating a Simple Blog.

## Endpoints

- `GET /posts`
- `GET /posts/:id`
- `GET /posts/:id/comments`

## Deployment

This project is configured for deployment on Vercel.
